# Faithful System Spec — OpenSpec change

This change captures a faithful specification of the existing StellarCrisisPHP codebase.

Usage:

- The `specs/` folder contains feature-scoped spec skeletons to be filled during reverse-engineering.
- `proposal.md`, `design.md`, and `tasks.md` are the main artifacts for this change.

Next steps:

1. Run the inventory (files + DB table list)
2. Extract schemas and algorithmic behavior into the corresponding spec files
3. Fill acceptance criteria and scenarios
