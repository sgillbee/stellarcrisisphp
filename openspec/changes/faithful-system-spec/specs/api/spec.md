## API & Backend Specification (skeleton)

### Overview

Document server-side endpoints, input parameters, expected behavior, authentication, and side effects. For the existing PHP app these will map to page entry points and form submissions.

### Example entries

- `game.php?action=move` — parameters, validation, DB updates, responses
- `update.php` — batch turn processing: inputs and outputs
