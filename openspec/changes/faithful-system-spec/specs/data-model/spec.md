## Data Model Specification (skeleton)

### Overview

This document will map SQL tables from `sc.sql` to application domain objects and list field-level types, constraints, and relationships.

### Initial tasks

- Extract table list from `sc.sql`
- For each table: name, columns, types, defaults, indexes, foreign keys
- Map tables to application pages and flows
